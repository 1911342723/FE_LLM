# 真实库 API 知识：隔离不忘 + 可定位编辑 + 按 import 路由

堵"合成玩具任务"质疑：把**真实 Python 库的标志性 API 惯用法**当作不同知识，每库一个 LoRA-ISO 块，在真实代码知识上验证整套机制。变量名作可变实例(teach/held 分割验证泛化)；动态选 base 最不会的库。真实 52M 代码模型，每块 163K 低秩参数。

选用库（base 最不会的 4 个）：`['numpy', 'pandas', 'torch', 'requests']`

## 1) 隔离不忘

各库用正确块 held-out acc：`{'numpy': 1.0, 'pandas': 1.0, 'torch': 1.0, 'requests': 1.0}`，平均 **100%**。

## 2) 可定位编辑

| 卸载目标库 | 目标降 | 旁观降 | 特异性 |
|---|---:|---:|---:|
| numpy | 100% | 0% | **+1.00** |
| pandas | 100% | 0% | **+1.00** |
| torch | 100% | 0% | **+1.00** |
| requests | 100% | 0% | **+1.00** |

平均编辑特异性 **+1.00**。

## 3) 按 import 路由

| 路由方式 | 正确率 |
|---|---:|
| import 触发词（真实可靠） | **100%** |
| 能量/置信度（学习化） | 44% |

## 结论

【真实库 API 知识·整套机制】在 ['numpy', 'pandas', 'torch', 'requests'] 上（base 最不会的真实库）：
  1) 隔离不忘：顺序学 4 个库，各库用正确块平均 acc 100%（隔离数学保证 Δ0）。
  2) 可定位编辑：卸载某库块 → 平均编辑特异性 +1.00（目标失效、旁观基本不动）。
  3) 按 import 路由：触发词(import) 100%（真实可靠）/ 能量 44%。
结论：整套机制（隔离不忘 + 可定位编辑 + 路由）在**真实库 API 知识**上成立——不只合成玩具；代码里 import 即天然路由信号，按 import 路由稳定可靠。

## 诚实边界

- 知识=库标志 API 惯用法（固定输出 + 变量名上下文泛化）；动态选 base 不会的库以保证'学新'有意义。
- 代码场景下 **import 是天然、可靠的路由信号**（按 import 路由 = 真实可用）；纯能量学习化路由较弱，是开放问题。
- 隔离类共性代价：参数随库线性增长。
- 机制验证（4 库、held-out 8 实例），不证规模。

图：`docs\reports\figs\code_real_library.png`
