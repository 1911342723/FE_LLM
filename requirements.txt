# ===== 核心运行时 =====
numpy>=1.21
python-dotenv>=1.0        # 加载 .env 配置

# ===== 教师语料生成（仅 data/teacher_gen.py 调 DeepSeek 时需要）=====
openai>=1.0               # 通过 OpenAI 兼容协议调用 DeepSeek 教师模型

# ===== 训练 / 评测 =====
torch>=2.0                # 训练 SeqEnergyNet（因果 PER）与对照 Transformer
matplotlib>=3.5           # 评测可视化（code_trace_viz 六联图 / per_synapse 热图）
