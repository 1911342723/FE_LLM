# -*- coding: utf-8 -*-
"""
translation 实验 —— 用最小自由能原理做中英双向翻译
===================================================
翻译是序列生成任务，输出空间无限，无法像算术那样"在所有答案里取 argmin 能量"。
但 FE-LLM 的核心思想被完整保留：

    1) 训练目标 = 最小化惊奇：
       交叉熵损失 -ln P(token | 上下文) 正是文档对惊奇度的数学定义
       Surprise = -ln P(Observation | Internal Model)。
       训练翻译模型最小化交叉熵，字面意义上就是在最小化系统的期望惊奇。

    2) 生成 = 能量递减解码：
       解码器每步输出词表能量 E(token) = -logit，贪心/束搜索选能量最低的词，
       逐字"滚落"，残余能量最低时输出 <EOS>。即文档第四步的能量递减解码器。

    3) 一个模型双向翻译：
       用目标语言标签 <2en>/<2zh> 控制翻译方向（多语言 NMT 标准做法）。

教师：DeepSeek 生成中英平行语料（teacher→student 蒸馏）。
设备：CUDA（RTX 5060）。
"""
