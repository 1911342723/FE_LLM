# -*- coding: utf-8 -*-
"""
arithmetic 实验 —— 用最小自由能原理做算术认知
===============================================
为什么选算术：它客观可验证，能最干净地展示 FE-LLM 的两条核心主张：
    1) 生成不是 softmax 概率预测，而是"在候选答案里取能量最低的吸引子"。
    2) 惊奇就是"错误答案对应的高能量"——看到 23+45=70 系统会惊讶，
       看到 23+45=68 能量趋近 0。

两个固化权重，正好对应 FE-LLM 架构里的两个可训练网络：
    - EnergyNet  : 惊奇/自由能网络。E(算式编码, 候选答案编码) → 标量能量(对=低,错=高)。
    - SolverNet  : 解码器。问题编码 → 直接回归出"答案意图向量"，再滚落到最近答案。

教师：DeepSeek 负责出题并给出标准答案（teacher→student 蒸馏管道）。
"""
