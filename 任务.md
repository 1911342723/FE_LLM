# FE-LLM 当前任务

更新日期：2026-06-15

## 当前主线

从 0 自建 FE-LLM v2，以**主动推理控制闭环**为核心（知道何时不该答 + 为何 + 会成长），不以预训练底座为主力。
- 主线设计：`docs/FE-LLM从0自建v2架构设计.md`
- 蓝图完成度：`docs/FE-LLM蓝图完成度.md`；术语表：`docs/FE-LLM术语表.md`

## 已完成（最近关键节点，细节见 经验.md / docs/reports/）

- [x] 主动推理控制闭环端到端打通 + 成长三层 + 工程化（生成层/学习式 NLU/网页·CLI demo）。
- [x] **N2 预训练底座线 → 封存**：step1（饱和 action 分类）+ step1b（有 headroom 的 9 领域 NLU）两面夹证，冻结 Qwen2.5-0.5B 句向量在 FE-LLM 控制/理解任务上均无杠杆。
- [x] **B1 统一 taxonomy 单一真相源**：`nlu/taxonomy.py` canonical 9 领域；感知/NLU/教师**三层贯通**派生（行为保持）。**B1 行为对齐(booking→route+date) 定案不做**——单槽位 booking 是有意机制 demo，差异已由 `LEGACY_SIMPLIFICATIONS` 显式记录。
- [x] **B2 系列 belief 真实价值地图（真实数据 CrossWOZ）**：动作类型选择 **无** headroom(−0.02) / 状态·领域追踪 **强**(+0.19) / 回复内容 grounding **强**(+0.15)。结论：belief 决定"谈什么/语境/内容"，不决定"怎么回应（动作类型）"。
- [x] **B2d/B2e 落地到 controller**：`active_domain` 跨轮追踪，多槽位领域**不必复述领域**即可主动补全 + 领域切换正确（B2e 修复了 B2d 被学习式 NLU 误判裸值架空的隐藏 bug）。窄门控守护寒暄/安全。
- [x] **多领域 belief 追踪 demo**：`fe_llm_multidomain_demo` 端到端实录展示上述能力（`docs/reports/fe_llm_multidomain_demo.md`）。
- [x] **分层预测编码 · 组合泛化裁决 → 正式封存（2026-06-15）**：用原则性实验（组合泛化=分层理论上唯一不可替代的好处，非凑 headroom）裁决。compare 任务未见组合：flat 0.652 ≥ hierarchical 0.637 ≥ hier_full 0.600（best_hier−flat=−0.014）→ **分层连理论主场都没赢扁平**。结合 V2-M1（分层还付 decode_loss 代价），**分层世界模型在本规模是过度设计，正式封存**。脚本 `world_model/compositional_generalization_eval.py`，报告同名 `{json,md}`。项目价值锁定在已验证的主动推理控制层。
- **全量 125 测试全绿**（裁决实验为独立 experiment，不影响回归集）。
- [x] **阶段总结（2026-06-15）**：`docs/FE-LLM阶段总结.md`——愿景/已验证成果/belief 价值地图/底座+分层封存定论/蓝图差距/方法论沉淀/复现。
- [x] **核心引擎构想·方案一 CAPCW（2026-06-15）**：`docs/FE-LLM核心引擎构想.md`——借鉴 Transformer 真正力量（内容寻址路由），把预测编码/自由能从"失败的固定纵向分层"搬到"内容寻址横向 slot 工作空间"+ 可溯源 + 可生长，保持六要素。含理论连接（attention 即推理 / slot-attention）、风险、最小验证方案。

- [x] **CAPCW 验证 → 第一个正面核心引擎结果（2026-06-15）**：in-context 键值绑定任务，唯一变量=世界状态结构。容量受限区间(d=32) CAPCW 决定性胜单向量（高 K 平均 +0.29），已封存分层近随机。诚实边界：容量充裕(d=64)时单向量够用、CAPCW 不需要——CAPCW 价值=绑定的容量效率，正中 FE-LLM 容量纪律的小规模主场。脚本 `world_model/capcw_binding_eval.py`，报告同名。文档 `docs/FE-LLM核心引擎构想.md` 第 9 节。

- [x] **CAPCW 阶段二a（2026-06-15）显式自由能形态 PCWorkspace**：`world_model/capcw.py`——slot 重建输入、路由由重建误差导出、弛豫降自由能、可溯源(responsibilities/error/F-trace)+穷则变生长钩子；单测 `tests/test_capcw.py`（5 条）。判据=PC 形态不丢绑定胜势：d=32 高 K 平均 CAPCW_PC−flat≈+0.27（与裸 slot-attn 持平）→ PASS。**全量 130 测试全绿**。

- [x] **CAPCW 阶段二b（2026-06-15）穷则变判定**：固定 K=5 扫 slot 数 M（d=32），accuracy 随 M 单调升、M≈K 饱和（M=2 0.40→M=5 0.89），M<K 平均 0.60→M≥K 0.89 转折 +0.29 → PASS：slot 数=绑定容量，穷则变前提成立。grow 钩子检测欠容量。脚本 `world_model/capcw_growth_eval.py`。caveat：grow 停止阈值需校准、训练期动态生长闭环待做。

- [x] **CAPCW 阶段三（2026-06-15）接控制层**：绑定+双输出（动作类型 + 回复内容=精确 value），d=32。动作类型 flat 0.556/capcw 0.911；**回复内容 flat 0.443/capcw 0.829（+0.386）→ PASS**。路由分离 bound/unbound≈0.48（surprise 信号）。**深意：CAPCW 控制层价值在内容/状态取回、不在动作类型——引擎层复现 B2 真实数据结论，两证互佐**。脚本 `world_model/capcw_action_eval.py`。

## CAPCW 四判定全正面（核心引擎方向确立）

内容寻址>单向量(一) / 显式自由能·可溯源不丢能力(二a) / slot 数=绑定容量→穷则变前提(二b) / 接控制层价值在内容取回·与 B2 一致(三)。

- [x] **CAPCW 三部（2026-06-15）推向真实 + 控制闭环**：
  - Part1 真实 CrossWOZ 绑定：real(可记忆) flat 0.981≫capcw 0.668；shuffle(真 in-context) flat 0.297≪capcw 0.883(+0.59)。**CAPCW=in-context 绑定引擎**（真实可记忆槽值不需要它）。`capcw_crosswoz_eval.py`。
  - Part2 穷则变自校准+按需分配：grow_m 随 K 增(2→3.5/4→6.5/6→7)、精度保持(≤0.04)→ PASS。`capcw_grow_dynamic_eval.py`。
  - Part3 surprise→动作闭环(无监督)：绑定良好 seed→balacc 0.881，均值 0.726(PARTIAL)，受绑定训练方差限制；机制成立。`capcw_surprise_action_eval.py`。

- [x] **CAPCW induction 负结果（2026-06-15）能力边界**：induction(...A B...A→B) flat 0.118/capcw 0.104 双双近随机→ CAPCW 集合式 slot 工作空间**擅长内容绑定、不自带序列相邻算子**。要做 induction 需在 token→slot 前补相邻/序列编码。诚实标定边界。`capcw_induction_eval.py`。

## 下一步（CAPCW 走向语言引擎，需补序列相邻）

- [ ] 给 CAPCW 补"序列相邻编码"（token 与前驱拼接 / PERBlock 先成 bigram）后重测 induction（独立判定）。
- [ ] 稳定 d 较小时的绑定训练（降方差，让 Part3 闭环更稳）。
- [ ] grow 阈值精调；surprise 门控接回真实 controller + 条件生成。
- [ ] B1·扩 9 领域：被容量卡住（低优先）。

## 容量纪律（不可违背）

- v2 不和通用 LLM 拼世界知识/翻译/开放问答（小模型容量已多次证伪）。
- 战场是机制：动作选择 / 多轮 surprise / 意图驱动短生成 / 可溯源 / 成长。
- 判定一个表示/机制有无价值前，先确认任务有 headroom（且诊断 headroom 来源），否则对照只会持平。

## 清理规则

- 已完成且超过当前阶段的历史不留本文件，沉淀到 `经验.md` / `docs/reports/`。
- 本文件只回答“现在做什么、下一步做什么”。
